@keywords of, is, has, prefix .

@prefix : <http://yosi.us/lambda#> . 
@prefix rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#> .
@prefix implies: <http://www.w3.org/2000/10/swap/log#implies> .

#((lambda (x) x) (lambda (y) y)) @a NeedsRun .

((lambda (Y) (Y Y)) 
(lambda (f) ((lambda (x) (f (x x))) (lambda (x3) (f (x3 x3)))))) @a NeedsRun .

#((lambda (I)
#  ((lambda (Y)
#    ((lambda (true)
#      ((lambda (false)
#
#((lambda (0) 
#  ((lambda (1)
#     ((lambda (2)
#        ((lambda (3)
#           ((lambda (4)
#
#((lambda (inc)
#  ((lambda (add)
#     ((lambda (mul)
#        ((lambda (pow) 
#
#
#((lambda (dec)
#  ((lambda (subtract)
#
#           ((add ((mul 2) 3)) ((pow 3) 3))
#            ((add 2) 3)
#
#  ) (lambda (m) (m dec)))
#) (lambda (c) ((c 
#   (lambda (z) ((z I) (inc z))))
#   (lambda (a) (lambda (x) 0)))))
#
#        ) (lambda (m) (lambda (n) (m n))))
#     ) (lambda (m) (lambda (n) (lambda (f) (m (n f)))))) 
#  ) (lambda (m) (m inc)))
#) (lambda (n) (lambda (f) (lambda (x) (f ((n f) x))))))
#
#           ) (lambda (f) (lambda (x) (f (f (f (f x)))))))           
#        ) (lambda (f) (lambda (x) (f (f (f x))))))         
#     ) (lambda (f) (lambda (x) (f (f x)))))      
#  ) (lambda (f) (lambda (x) (f x))))
#) (lambda (f) (lambda (x) x))) 
#
#      ) (lambda (x) (lambda (y) y)))
#    ) (lambda (x) (lambda (y) x)))
#  ) (lambda (f) ((lambda (x) (f (x x))) (lambda (x) (f (x x))))))
#) (lambda (x) x))
#@a NeedsRun .
